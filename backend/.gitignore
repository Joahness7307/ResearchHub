# === BACKEND ONLY ===
# Everything from root already applies, so just add backend-specific ones

# Build output (TypeScript/Node)
dist/
build/
*.tsbuildinfo

# Local DB or config
.env
.env.local
config/*.local.json

# Nodemon / PM2
nodemon.json
.pm2/

# Logs folder
logs/
*.log

# Uploads folder (if you store uploaded files locally during dev)
/uploads
/public/uploads